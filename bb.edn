{:paths ["src" "test"]
 :tasks {test {:doc "Run the cljc contract + behavioral test under babashka"
               :extra-paths ["test"]
               :requires ([orcid.main-test])
               :task (let [{:keys [fail error]} (clojure.test/run-tests 'orcid.main-test)]
                       (System/exit (if (pos? (+ fail error)) 1 0)))}}}
